import React, { FC } from 'react';
export interface Props {
    isActive?: boolean | null;
}
export declare const UserActivityManager: FC<React.PropsWithChildren<Props>>;
export default UserActivityManager;
